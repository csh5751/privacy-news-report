{
  "name": "privacy-news-feedback",
  "private": true,
  "scripts": {"deploy": "wrangler deploy", "db:init": "wrangler d1 execute privacy-news-feedback --remote --file schema.sql"},
  "devDependencies": {"wrangler": "^4.0.0"}
}
